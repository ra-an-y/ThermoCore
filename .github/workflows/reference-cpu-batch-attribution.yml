name: Reference CPU Batch Attribution

on:
  pull_request:
    paths:
      - "Performance/**"
      - "Framework/**"
      - "Materials/**"
      - ".github/workflows/reference-cpu-batch-attribution.yml"
  workflow_dispatch:

jobs:
  evaluate-reference-cpu-batch-attribution:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Run CPU batch attribution evaluation
        run: >-
          dotnet run
          --project Performance/ThermoCore.ReferenceBatchAttribution.csproj
          --configuration Release
